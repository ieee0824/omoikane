const ffi = require("./node_ffi_napi");

// Minimal placeholder bridge sample: a real adapter would map Puppeteer commands
// onto the exported C ABI and the CDP transport/session layer.
console.log("Use the ffi-napi sample as the low-level transport hook for Puppeteer.");
